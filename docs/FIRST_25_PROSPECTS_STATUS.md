# First 25 Prospects Status

A researched first prospect seed list has been added to the sales launch kit.

## File added

```text
sales_launch_kit/first_25_prospects_canada_seed.csv
```

## What it includes

The seed list includes 25 Canadian agency prospects across:

```text
Toronto
Vancouver
Calgary
Edmonton
Gibsons
```

Agency categories include:

```text
local SEO
reputation management
PR
content marketing
web design and local SEO
growth marketing
```

## How to use it

1. Open the CSV.
2. Sort by `fit_score` descending.
3. Start with the top 10.
4. Verify each agency website and contact route manually.
5. Personalize using the `personalization_hook` column.
6. Offer one sample brief using the `sample_client_niche` column.
7. Track status in the same CSV or copy rows into `prospect_target_list_template.csv`.

## Next operating step

Run the Sales Launch Kit workflow again so the generated kit includes the researched seed list.

Expected generated file:

```text
first_25_prospects_canada_seed.csv
```

Expected manifest version:

```text
v0.4-prospect-seed
```
