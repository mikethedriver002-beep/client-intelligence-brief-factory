# Workflow Notes

Use `.github/workflows/brief-factory-run.yml` for manual GitHub Actions runs.

Recommended first run:

- `run_mode`: `full_delivery_packet`
- `enable_network`: `false`
- `strict_qa`: `true`

Network fetching should stay off until you have real reviewed RSS/public sources in the registry.
